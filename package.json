{
  "name": "rocketflare-plugins",
  "version": "2.0.1",
  "private": true,
  "description": "The first-party plugins for Rocketflare — copied into an app, never installed from npm.",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "release": "node scripts/release.mjs",
    "release:check": "node scripts/release-check.mjs"
  }
}
